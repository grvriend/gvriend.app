# Development

## Prerequisites
- Ruby & Jekyll

## Setup
1. Install Ruby dependencies:
   ```bash
   bundle install
   ```

## Running Locally
To run the site:
```bash
bundle exec jekyll serve
```
This will start the Jekyll server at `http://localhost:4000`.
